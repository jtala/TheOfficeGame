import React from "react";
import "./Directions.css";

const Directions = () => (
	<header className = "header">
		<h1>The Office Game</h1>
		<h2>Click on an Office Character, but be careful not to select them twice!</h2>
	</header>
);

export default Directions;